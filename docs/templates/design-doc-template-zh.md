# Design: <topic>

**日期**: YYYY-MM-DD
**Owner**:
**状态**: Draft / Reviewing / Locked
**源项目 / 分支**:
**相关调研 / 代码讲解 / review**:

## 修订记录

| 版本 | 日期 | 作者 | 摘要 |
|---|---|---|---|
| v0.1 | YYYY-MM-DD |  | 初版设计。 |

## 1. 摘要

- 这个设计解决什么问题:
- 选择的方向:
- 预期结果:
- AI Agent 应该能根据本文直接实现什么:

## 2. 背景和目标

- 当前状态:
- 痛点 / 动机:
- 目标:
- 非目标:
- 约束:
- 成功标准:

## 3. 当前系统对齐

| 区域 / 模块 | 当前行为 | 对设计的影响 |
|---|---|---|
|  |  |  |

## 4. 候选方案

| 方案 | 核心思路 | 优点 | 缺点 / 风险 | 判断 |
|---|---|---|---|---|
| A |  |  |  |  |
| B |  |  |  |  |
| C |  |  |  |  |

## 5. 选择

**选择的方案**:

**为什么选它**:

**为什么不选其他方案**:

- A:
- B:
- C:

**后果 / 取舍**:

- 什么会变简单:
- 什么会变困难:
- 可能引出的后续决策:

## 6. 详细设计

### 6.1 架构 / 流程

```text
[component / caller]
  -> [new or changed module]
  -> [state / storage / external system]
```

### 6.2 数据 / 状态模型

- 新增或修改字段:
- 归属 / 生命周期:
- 迁移 / 回填:
- 兼容性:

### 6.3 API / CLI / 接口变更

- 对外接口:
- 内部接口:
- 输入校验:
- 输出 / 错误形态:

### 6.4 关键流程

| 流程 | 入口 | 步骤 | 结果 |
|---|---|---|---|
|  |  |  |  |

### 6.5 错误处理和边界

- 预期错误:
- 重试 / 超时行为:
- 部分失败行为:
- 并发 / 顺序:
- 幂等性:

### 6.6 可观测性和运维

- 日志:
- 指标:
- 告警:
- 调试命令 / 查询:
- 回滚 / 禁用开关:

## 7. 分阶段实现与验证计划

> 实现和测试放在一起写。每个阶段都要具体到 AI Agent 可以实现、验证，并在阶段边界停下来，不需要临时发明范围。

### Phase 1: <阶段名称>

**目标**:

**实现范围**:

- [ ] 文件 / 模块:
- [ ] 文件 / 模块:

**数据 / migration 改动**:

- [ ] 

**Agent 执行约束**:

- 必须遵守:
- 禁止做:
- 不确定时先问:

**本阶段验证**:

- 自动化测试:
- 手工 / workflow 验证:
- 回归检查:
- 失败 / 边界检查:

**退出标准**:

- [ ] 

### Phase 2: <阶段名称>

**目标**:

**实现范围**:

- [ ] 文件 / 模块:
- [ ] 文件 / 模块:

**数据 / migration 改动**:

- [ ] 

**Agent 执行约束**:

- 必须遵守:
- 禁止做:
- 不确定时先问:

**本阶段验证**:

- 自动化测试:
- 手工 / workflow 验证:
- 回归检查:
- 失败 / 边界检查:

**退出标准**:

- [ ] 

### Phase 3: <阶段名称>

**目标**:

**实现范围**:

- [ ] 文件 / 模块:
- [ ] 文件 / 模块:

**数据 / migration 改动**:

- [ ] 

**Agent 执行约束**:

- 必须遵守:
- 禁止做:
- 不确定时先问:

**本阶段验证**:

- 自动化测试:
- 手工 / workflow 验证:
- 回归检查:
- 失败 / 边界检查:

**退出标准**:

- [ ] 

### 整体验收

| 验收领域 | 验证内容 | 命令 / 方法 | 合并前是否必须 |
|---|---|---|---|
| 单元 / 组件 |  |  | Yes / No |
| 集成 / workflow |  |  | Yes / No |
| 端到端 / 运维 |  |  | Yes / No |
| 回归测试 |  |  | Yes / No |
| 回滚 / 兼容性 |  |  | Yes / No |

**必要测试数据 / fixtures**:

**性能 / 规模检查**:

**向后兼容检查**:

**失败注入 / 负向测试**:

## 8. 发布和回滚

- 发布顺序:
- Feature flag / 配置开关:
- 部署顺序:
- 发布期间监控:
- 回滚步骤:
- 如果回滚，数据如何清理:

## 9. 风险和缓解

| 风险 | 影响 | 缓解方式 | 测试 / 信号 |
|---|---|---|---|
|  |  |  |  |

## 10. AI Agent 交接检查清单

- [ ] 明确列出了要改的文件 / 模块。
- [ ] 每个阶段都把实现范围、验证方式和退出标准放在一起。
- [ ] 整体验收写清楚必须执行的命令或手工检查。
- [ ] 高风险决策标成“不确定时先问”。
- [ ] 非目标足够明确，能防止实现时扩大范围。

## 11. Open Questions

- [ ] 
